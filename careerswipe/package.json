{
  "name": "careerswipe-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "node -e \"const { spawn } = require('child_process'); spawn('npm', ['run', 'dev'], { cwd: 'backend', shell: true, stdio: 'inherit' }); spawn('npm', ['run', 'dev'], { cwd: 'frontend', shell: true, stdio: 'inherit' });\""
  }
}
